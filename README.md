# CBS
CBS (Customer Billing System) is a C programming project designed for small retail businesses. The system supports two types of users: Admin (with full access to system functionalities) and Customer (with limited access).
